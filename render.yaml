services:
  - type: web
    name: asset-dashboard
    runtime: python
    buildCommand: "pip install -r requirements.txt && python updater_script.py"
    startCommand: "gunicorn app:app"
    envVars:
      - key: PYTHON_VERSION
        value: 3.10.0
